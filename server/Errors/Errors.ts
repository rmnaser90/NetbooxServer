const Errors = {
  BAD_REQUEST: { err: true, msg: "something went wrong" },
  WRONG_PASSWORD: { err: true, msg: "wrong password" },
  INVALID_EMAIL: { err: true, msg: "email doesn't exist" },
  INVALID_PASSWORD: { err: true, msg: "Enter a valid password" },
};

export default Errors;
